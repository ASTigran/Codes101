#include<stdio.h>
#include<math.h>
int main()
{
    int x,y,p,a,b;
    scanf("%d%d%d%d%d",&x,&y,&p,&a,&b);
    int d= pow(x-a,2)+pow(y-b,2);
    int p0=p*p;
    if (p0>=d){
        printf("AVENGERS");
    }
    else printf("THANOS");
    //  Insert your code here.
    
    return 0;
}
